<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reihan.hira">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".project.FormProjectActivity"></activity>
        <activity android:name=".hire.FormHireActivity" />
        <activity android:name=".project.DetailProject" />
        <activity android:name=".detailWorker.ProfileWorkerActivity" />
        <activity android:name=".webview.WebPortoActivity" />
        <activity android:name=".AppIntroScreen" /> <!-- <fragment android:name=".ProfileScreen" /> -->
        <activity android:name=".profile.FormProfileActivity" />
        <activity android:name=".register.Sign_up" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>